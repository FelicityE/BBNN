CMAKE_MINIMUM_REQUIRED (VERSION 3.22)
PROJECT (BBNN)
SET (CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_SOURCE_DIR}/make)

INCLUDE_DIRECTORIES (../include/)

FILE (GLOB SOURCE_CODE "../src/*")

ADD_EXECUTABLE(BBNN ../src/main.cpp ${SOURCE_CODE})